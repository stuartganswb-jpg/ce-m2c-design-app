---
name: back-half-fulfillment
description: "Back-half fulfillment effort (post-CPQ order lifecycle): the 3-workflow / 4-phase plan and Phase 1 implementation status"
metadata: 
  node_type: memory
  type: project
  originSessionId: 51fa5a68-4c5f-4589-bd80-48db20ff7e21
---

`BACK_HALF_FULFILLMENT_BRIEF.md` (repo root) defines everything AFTER the CPQ quote → NetSuite → confirmed SO on RTG Dispatch (front half is tested/working). Three workflows: **A** standard in-house lifecycle, **B** outsourced stock & replenishment, **C** outsourced finishing logistics (weekly batch). Builds on the shared-`orderKey` model from [[work-order-contract-impl]] (already merged to main).

**Phase 1 (close Workflow A) SHIPPED to prod 2026-06-16 — `main` fast-forwarded to `ef25397` and pushed (Vercel auto-deploy → 4cosworkcenter.com).** Repo: github.com/stuartganswb-jpg/ce-m2c-design-app. Builds clean (`CI=false npm run build`); logic test 9/9 + full doc-trace verified; NOT yet exercised live against Firestore (test on the live app + set `activeFloorDailyCapacity` in `fin_config/settings`, default 200, before relying on it). (Note: three sessions shared one working copy mid-session, so this commit briefly landed on `feat/cpq-combo-photos`; removed via rebase — all branches verified cleanly separated. Prod state after the dust settled: `main` = `c5c3374` (2026-06-16) = Phase 1 (`ef25397`) + CPQ "rule-disabled steps authoritative" (`8358043`, cherry-picked from `feat/production-packet` `a5eae64`) + CPQ rule-builder IF-dropdown enrichment (`5e5c6f7`: exposes `customData.isReturnBracket` + bracketType/projection/bpOrientation/armThickness/feeType + `collections`) + read-only **ERP Mapping Audit** tab (HQ `11.2`, `d8cd266`: lists Approved_Designs parts missing `netSuiteInternalId`/`itemId`, file `src/components/HQ/ErpMappingAudit.js`) + CPQ rule-engine hardening (`875b783`: the disable-step rule eval now maps STYLE_SWAP option ids→library partId so it can read the selected part's `customData` — root cause of the end-return skip rule not firing; also tolerant step-title match storing the real title, `customData` fallback for unprefixed condition fields, honors NOT_EQUALS/CONTAINS, gated `localStorage.cpqRuleDebug='1'` console trace). End-return skip rule lives in `system/cpq_rules`; the bracket actually SELECTED must itself have `customData.isReturnBracket===true` (variants too, not just the base). ROOT CAUSE & FIX (CONFIRMED WORKING on prod, final `c5c3374`): hardware STYLE_SWAP bracket options carry a PROJECTED name as BOTH partId & partName (e.g. `FICERA1001 CEILING BRACKET LEFT`) with NO stored source-part id; the rule eval resolved selections by doc id only → stub → couldn't read `customData` → rule never fired. Fix chain (CPQTab.js rule-eval `useEffect`): resolve the selected option to its library part by (a) exact `id/itemId/itemName/legacyErpId`, then (b) **longest part-code prefix** of the projected name (`FICERA…`→FICERA, stays distinct from center `FICPBA…` / end-cap `FIEC…`); plus tolerant step-title match (store real title), `customData` fallback for unprefixed condition field, and EQUALS/NOT_EQUALS/CONTAINS. Debug log + the on-screen "Step X disabled because Y is TRUE" notice both removed (appliedRules still recorded in the quote). NOTE the pricing/BOM resolver (`CPQTab.js ~line 1030`) is still doc-id-only (falls back to styleOpt.partName/price) — same legacy-vs-ERP id mismatch the [[program-prints-asset-gallery]] ERP Mapping Audit surfaces; normalizing itemId===docId (or storing a source id on projected options) would harden it. To re-debug a non-firing disable rule, the gated `localStorage.cpqRuleDebug` trace is GONE — re-add temporarily if needed. End-return skip rule = IF `customData.isReturnBracket` EQUALS TRUE THEN `UI.disableStep` = "Left End Treatment" / "Right End Treatment" (two rules, stored at `system/cpq_rules`). Return-bracket flag is `manufacturingSpecs.customData.isReturnBracket` (boolean). The combo-photos *feature* itself (`feat/cpq-combo-photos` `cdc1748`) is NOT in prod — separate decision.):
- **A1 — Pick/Pack trigger (the showstopper fix).** PickPack filters `sentToPickPack===true` but nothing ever set it true → pick queue was always empty. Now: shop "Start" on the custom job calls `releaseSiblingToPickPack(order)` (new in `Shared/workOrderContract.js`) from `ShopFloor.handleStartProcess`, flipping the sibling fin WO's `sentToPickPack:true`. Small-only orders (no custom sibling) release at split via `sentToPickPack: !hasCustom` in `RTGDispatchTab.autoSplitSalesOrder`.
- **A2 — two-label staging handshake.** Replaced the fuzzy `stagingScanMatches` (`includes`) with exact `resolveByExactKey`. PickPack staging now takes two scans (small + custom); for orders with a custom half they must be the SAME orderKey exactly AND `customFabStatus==='Complete'`; small-only skips the 2nd scan. Sets `pickStatus:'Staged_Ready_For_Finishing'` + `stagingStatus:'MATCHED'`.
- **A3 — Setup Queue green + push.** `SetupQueue` cards go green and show "✓ Push to Active Floor" when `stagingStatus==='MATCHED'` (calls existing `stageToFloor` → `currentPhase:'Painting'`). Existing Start Setup/Stage to Floor preserved for non-matched orders.
- **A4 — Active Floor daily cap (PIECES).** `stageToFloor` now blocks if floor load + this order > cap. Cap = `sysConfig.activeFloorDailyCapacity` (default **200**, lives in `fin_config/settings`; default added in `FinishingFloor.js`, threaded into SetupQueue). Capacity meter in queue header.

**Decisions locked** (user-approved): outsourced = first-class line lane keyed on `manufacturingSpecs.isInHouse===false` (Phase 3); explicit `stockPolicy` field default-inferred from /P vs /EP# (Phase 3); Assembly Build (not Work Order) for raw→finished (Phase 3); capacity unit = **pieces**; small-only releases to pick **at split**.

**Deferred / caveats:**
- Real ZPL label printing = **Phase 2** (`printZebraLabel` in PickPack still a stub — now just logs orderKey; handshake is testable by hand-keying orderKey into the scans).
- Manual paths still `sentToPickPack:false` on purpose (SetupQueue "Direct Order Intake" `handleCreateOrder`; RTG `pushToFinishing`) — they have empty `partsList`, so not auto-released. Flag if they need a manual "release to pick".
- **Custom-only orders create NO fin WO** (split only makes one when `hasSmall`), so they aren't tracked through pick/staging — pre-existing gap, left as-is.
- A4 cap gates **all** `stageToFloor` incl. the legacy "Stage to Floor" button (single enforcement point). If a manual order > cap, it's blocked — tune the default if this bites.
- Phases 2–4 not started. See [[cpq-hw-render-shipped]] / [[program-prints-asset-gallery]] (same app, shipped, minimal overlap — only ShopFloor.js shares heavy code).
