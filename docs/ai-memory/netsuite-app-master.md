---
name: netsuite-app-master
description: "Making the App the NetSuite master (write-back) + CPQ→NetSuite BOM push correctness; audit findings, Phase A shipped, Phase B plan"
metadata: 
  node_type: memory
  type: project
  originSessionId: 51fa5a68-4c5f-4589-bd80-48db20ff7e21
---

Goal (user, 2026-06-17): the **App becomes the NetSuite master** — curate items/assemblies in the App, push changes back to NetSuite on sync; NetSuite must NOT overwrite App curation; App-only fields (collection, cpq tags, `customData`) that don't exist in NetSuite must NOT fail the write. Eventually create brand-new items & assemblies in the App and push to NetSuite (after legacy items are migrated out of NetSuite). User chose scope = protective fixes + App→NetSuite write-back (NOT create-new yet).

**Audit findings (two parallel agents, read-only):**
- CPQ→NetSuite estimate push (`ERPPushPullTab.resolveJobLines`/`handlePushToNetSuite`): price **roll-up into the per-flow non-inventory item (`flow.nsRollupItemId`, fallback `61502`) WORKS**, but it **silently DROPPED every STYLE_SWAP bracket/finial/backplate** because it matched optIds/projected names against doc-id only.
- Sync (`NetSuiteSyncTab`): **PULL-only / NetSuite-authoritative**. Only App→NS writes anywhere = `LibraryTab.handlePushUpdatesToNetSuite` (PATCH existing item, 4 fields) + `AdminTab.handleCreateRollupItem` (POST nonInventory). The proxy `functions/index.js` is a **pure pass-through → NetSuite rejects the WHOLE record on any bad/unknown field** (no per-field tolerance). Pull rebuilt `customData` from 3 keys → **wiped curated tags**.

**Phase A — SHIPPED to prod 2026-06-17 (`main ded5cbd`):**
- `NetSuiteSyncTab.handleSyncItems`: deep-merge `customData` on pull (`{...newSpecs.customData, ...existingSpecs.customData}`) so App-set tags survive a sync. (Safe to run Sync Items again.)
- `ERPPushPullTab.resolveJobLines`: resolve a CPQ selection → real library part via exact (id/itemId/itemName/legacyErpId) → longest part-**code-prefix** (brackets: `FICERA1001…`→FICERA) → full item-name (poles/rings); handles backplate `__sub`; skips `__finish`. Components no longer dropped; price still rolls up.
- Open Q for user: estimate currently prices components at base + rollup absorbs the balance (total = quote). If they want ALL price on the rollup line + components at $0, it's a small change.

**Phase B v1 — SHIPPED to prod 2026-06-17 (`main a9f3a6c`):** `NetSuiteSyncTab.handlePushItemsToNetSuite` + a new "⬆ Push Items → NetSuite (App is master)" button. Bulk-PATCHes each brand item that has a `netSuiteInternalId` with the App's core curated fields only — `itemid`←legacyErpId, `displayname`←itemName, `custitem9`←basePrice, `weight`. Tolerant: record-type retry (inventory↔assembly, reusing LibraryTab's pattern) + drop-to-core (itemid+displayname) so an odd field can't fail the row; per-item loop so one failure can't halt the run; per-item error logged. Tags NOT sent (App-only by design). **UNTESTED live** — user should push ONE item via the LibraryTab per-item push first, then run the bulk button; watch the sync log for the per-item result. NS custom-field id map (for a future tag write-back): product_type=`custitem_bit_product_type`, collection=`custitem_bit_itemcollection`, watchlist=`custitem_bit_watchlist`, projection=`custitem_bracket_projection`, basePrice=`custitem9`, "sync to CPQ" flag=`custitem_sync_to_cpq`. DEFERRED: tag/list-field write-back (needs list-value internal-id mapping — user will add NS custom fields later) + create-new items/assemblies incl. BOM (option 3).

Data-alignment tool: the **Mass Update CSV template** (`LibraryMassUpdateTab`, shipped `main 351bc50`) now exposes the identity/mapping fields for bulk correction — Item ID, NetSuite Internal ID, writable Legacy ERP ID, Bracket Type/Mount, + editable Item Name & Part Class + client pricing (Client Customer ID / Client SKU / Client Cost→`price` / Client Sales Price→`clientSalesPrice`; `clientPricing` is an array, so export shows the FIRST entry and import UPSERTS by customer id, preserving others — `main 19d52d2`) (match keyed on the "ID (DO NOT EDIT)" doc id; identity/pricing cells written only when non-empty). Pricing model (CONFIRMED INTENTIONAL 2026-06-17, do NOT "fix"): the editor's "Client Cost" field is stored as `clientPricing[].price`, and ERPPushPullTab rates the customer line at `cp.price` — i.e. CPQ quotes at the client's COST now, which is correct. `clientSalesPrice` is captured but not yet consumed — reserved for the FUTURE customer portal, where a customer chooses to quote at their cost or at their (customer's) sales price. Don't switch the push to clientSalesPrice. This is how the user aligns `itemId`/`legacyErpId`/`netSuiteInternalId` so resolution + the Phase B write-back work cleanly.

PickPack Bin Count (cycle count → NetSuite inventory adjustment, `main 7bf8eb4`): added HQ-style search header (Category + Collection filters + multi-field search name/SKU/itemId/bin + Clear). Adjustment push fixed to skip unmapped items (was sending the Firestore doc id as the NS item ref → 400) + surface the real NS error. Adjustment account set to **254** (real NetSuite Inventory Adjustment Account internal id, `main d39ce25`). Then the payload STRUCTURE was wrong for REST (`main 5c0e899`): the line sublist was `inventoryList` (SOAP-era name → REST ignored it → 400 "you must enter at least one line item"); fixed to `inventory`, moved `location` onto each line, bin now `binNumber:{refName: binName}` (was `receiptInventoryNumber` string), and line `adjustQtyBy` = `inventoryDetail.quantity` = bin-assignment qty (same SIGNED delta so the detail reconciles). CONFIRMED WORKING live 2026-06-17 (+1 on "1\" Metal Ball Finial Textured", bin U S16-E2R-R2, sub 2/loc 17 pushed OK). Then added an **Adjustment Memo** textarea to the Count Synapsis Review modal (`main 6227598`): on push the NS adjustment header `memo` = `Cycle count by <operator.name> — <note>`; memo also appended to wms log; cleared after success. Then (`main 7175e86`): (1) count now includes OUTSOURCED/bought inventory — dropped the `specs.isInHouse !== false` clause from the count filter (was silently hiding bought items; you count stock whether you make or buy it); (2) the Bin Location cell is now EDITABLE per row (`binEdits` state) — typing a bin different from the item's stored one marks it changed, and on push `ensureBinExists(bin, location)` POSTs `record/v1/bin` (idempotent: a duplicate/already-exists error is swallowed as success) then writes the bin back to the item (`updateDoc Approved_Designs, manufacturingSpecs.binLocation`) so CONVERT/plating flows use it, then the adjustment posts into it. Review modal shows a Bin column with a "(new)" tag. `bin` is a NET-NEW record type. CONFIRMED WORKING live 2026-06-17: adjustment IA25969 created bin PRD-081 and placed qty 10 into it (item H1-138EC, status Good) — so `binNumber:{refName: binName}` DOES place stock correctly; refName is NOT ignored. The user's "it didn't go in the bin" was a misread of the item's **Bin Numbers** tab, which lists PREFERRED/associated bins (a separate item-master setting) — empty there is fine; the stock still physically sits in PRD-081 per the transaction's Inventory Detail. I briefly over-corrected to a SuiteQL bin-id resolver (`main 85b3c03`) but REVERTED it (`main a454c6f`) since refName works and the resolver added an untested `bin` table dependency. So the live code = `ensureBinExists` (create new bin, idempotent) + `binNumber:{refName}` placement in BOTH the adjustment and the assembly build. Lesson: `binNumber:{refName}` is correct; don't switch to internal-id. Possible future nicety (NOT requested): also set the new bin as the item's PREFERRED bin (the Bin Numbers sublist on the item) so picks default to it. (b) if `inventoryDetail` errors on an item that isn't bin-tracked, make the detail conditional/drop it for those.

PickPack now has a dedicated **TRANSFER** tab (`main 0068dca`) — separate from COUNT to avoid confusion. Pick item → scan source bin → qty → scan/enter dest bin (created via `ensureBinExists` if new) → `pushBinTransfer` POSTs `record/v1/bintransfer` `{ location, memo, inventory.items:[{ item, inventoryDetail:{ quantity, inventoryAssignment.items:[{ binNumber:{refName:from}, toBinNumber:{refName:to}, quantity }] } }] }`. Moves qty between bins at one location, total on-hand unchanged. `bintransfer` is NET-NEW — UNTESTED; `toBinNumber`/`inventoryAssignment` field names may need 1 iteration from the real error. PickPack tab order now: QUEUE, PACKING, COUNT, CONVERT, TRANSFER, GALLERY, MESSAGING.

Cross-ref: the projected-name / `itemId≠docId` resolution crux is the same one in [[back-half-fulfillment]] (CPQ end-return rule fix) and surfaced by the ERP Mapping Audit tab (HQ 11.2).
