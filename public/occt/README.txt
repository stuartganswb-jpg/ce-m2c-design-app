occt-import-js 0.0.23 (https://github.com/kovacsv/occt-import-js) — OpenCascade compiled to WebAssembly, LGPL-2.1.
Vendored here so the STEP reader loads as a plain script on first use (Shared/stepImport.js), never bundled.
Keep in step with the devDependency of the same version, which the node harness scripts/stepImport.test.mjs uses.
